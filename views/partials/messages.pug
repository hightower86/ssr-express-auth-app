if typeof errors != 'udefined'
each error in errors
  .alert.alert-warning.alert-dismissible.fade.show(role='alert')
    strong= error.msg 
    button.close(type='button', data-dismiss='alert', aria-label='Close')
      span(aria-hidden='true') &times;